# Web框架
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# 数据库
sqlalchemy==2.0.23
pymysql==1.1.0  # 保留作为备用
psycopg2-binary==2.9.9  # PostgreSQL 驱动
pgvector==0.2.4  # pgvector 支持
cryptography==41.0.7
alembic==1.12.1

# 数据验证
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0

# JWT和安全
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2

# Redis
redis==5.0.1

# 邮件
aiosmtplib==3.0.1

# HTTP客户端
httpx==0.25.1

# 工具库
pyyaml==6.0.1

# === RAG 系统依赖 ===

# PyTorch（GPU 版本 - 优先安装）
# 如果没有预安装，请先运行：
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0
torchvision>=0.15.0

# LangChain 核心
langchain==0.1.0
langchain-community==0.0.13
langchain-core==0.1.10

# 多语言 Embedding 和 Reranker（BGE-M3 生态）
FlagEmbedding==1.2.5
sentence-transformers==2.2.2

# Transformers（Tokenizer）
transformers==4.35.2
tokenizers==0.15.0

# 向量检索
# 注意：已迁移到 PostgreSQL + pgvector，不再需要 FAISS
# Embedding/Reranker 模型在 GPU 上运行（通过 PyTorch）
numpy==1.26.4

# PDF 解析
pymupdf==1.23.8
PyPDF2==3.0.1

# 其他工具
pillow==10.1.0

